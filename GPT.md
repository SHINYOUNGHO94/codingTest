# GPT.md

문제집 자료나 사용자 풀이 리뷰를 검토하기 전에 `AGENTS.md`를 읽는다.

## Codex의 역할

Codex는 Claude Code가 만든 결과를 독립적으로 검토하고 보완한다. Codex가
문제 출제를 주도하지 않는다.

## 문제 세트 검토

새로운 문제를 공개하기 전에 다음 항목을 확인한다.

1. 언어와 목표 직무가 명확한지 확인한다.
2. 요구사항, 예시, 제약 조건, 완료 조건이 명확한지 확인한다.
3. 공개 테스트가 정답을 노출하지 않으면서 핵심 동작을 설명하는지 확인한다.
4. 숨겨진 테스트가 의미 있는 엣지 케이스를 다루는지 확인한다.
5. 정답 코드를 독립적으로 검증한다.
6. 난이도, 중복 여부, 채용 대비 목적과의 적합성을 확인한다.
7. 가능한 `type-check`, `lint`, `test`를 실행한다.
8. 문제 오류와 구현 오류를 분리하여 보고한다.

## 사용자 풀이 리뷰 검토

Claude Code의 풀이 분석을 `AGENTS.md`의 리뷰 순서에 따라 다시 확인한다.

- 실패하는 입력이 실제로 재현되는지 확인한다.
- 테스트가 놓친 엣지 케이스를 추가로 찾는다.
- 복잡도 분석이 정확한지 확인한다.
- TypeScript에서는 타입 안전성과 입력값 변경 여부를 확인한다.
- Python에서는 런타임 오류 가능성과 자료구조 선택을 확인한다.
- 다음 변형 문제나 보완 학습 주제가 오답 원인과 연결되는지 확인한다.
- 다음 문제 수가 사용자의 현재 숙련도에 맞는지 확인한다.

`solutions/user/`를 직접 수정하지 않는다. 사용자가 명시적으로 요청하지 않는
한 수정된 전체 풀이를 공개하지 않는다.
