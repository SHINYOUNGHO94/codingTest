# AGENTS.md

## 목적

이 저장소는 프론트엔드, 백엔드, 풀스택 엔지니어 이직 과정의 코딩테스트를
준비하기 위한 문제집이다.

사용자는 TypeScript와 Python으로 직접 문제를 푼다. Claude Code는 사용자의
명령을 받아 문제와 출제자 전용 자료를 만든다. Codex는 Claude Code가 만든
자료와 AI 리뷰 결과를 독립적으로 검토하고 보완한다.

## 기본 원칙

1. 파일을 수정하기 전에 요구사항을 이해한다.
2. 모호한 조건은 명시적으로 적는다.
3. 구현 전에 완료 조건을 정의한다.
4. 유지하기 쉬운 가장 단순한 해결책을 우선한다.
5. 관계없는 파일을 수정하거나 리팩터링하지 않는다.
6. 문제를 추가하거나 수정한 뒤 가능한 `type-check`, `lint`, `test`를 실행한다.
7. 실패하거나 실행하지 못한 검증을 숨기지 않고 보고한다.
8. 문제 자체의 오류와 사용자 풀이의 오류를 구분한다.

## 역할 분담

### Claude Code

Claude Code는 사용자의 명령을 받아 다음 작업을 수행한다.

- 언어와 직무에 맞는 학습 계획 작성
- 문제, 시작 코드, 공개 테스트 작성
- 숨겨진 엣지 케이스 작성
- 정답과 해설 작성
- 난이도 조절
- 이전 오답 개념의 변형 문제 작성
- 사용자 풀이 제출 후 상세 리뷰 초안 작성

### Codex

Codex는 문제를 직접 출제하는 주 담당자가 아니다. Claude Code의 결과를
검토하고 보완한다.

- 문제 요구사항의 명확성 검토
- 언어와 직무에 맞는 문제인지 확인
- 공개 테스트와 숨겨진 테스트의 검증 범위 확인
- 정답 코드의 오류 검증
- 난이도와 중복 문제 확인
- 가능한 `type-check`, `lint`, `test` 실행
- Claude Code가 작성한 사용자 풀이 리뷰의 누락과 오류 검토
- 오답 기록을 바탕으로 다음 학습 주제 제안

풀스택 문제를 검토할 때는 `docs/fullstack-tasktree-baseline.md`도 읽는다.
문제 생성과 학습 운영을 이어받을 때는 `docs/training-workflow.md`, `docs/problem-roadmap.md`,
`docs/claude-problem-authoring.md`를 함께 읽는다.

### 사용자

- `solutions/user/` 아래의 풀이 파일만 직접 작성한다.
- 처음에는 정답과 해설을 열어보지 않는다.
- 풀이를 제출한 뒤 AI 리뷰를 요청한다.
- 시간 복잡도, 공간 복잡도, 엣지 케이스를 직접 설명한다.

## 반드시 지킬 규칙

- 사용자가 풀이를 제출하기 전에 정답 전체를 공개하지 않는다.
- 사용자를 대신하여 `solutions/user/` 아래의 파일을 수정하지 않는다.
- 사용자가 막히면 한 번에 하나씩 단계별 힌트를 제공한다.
- 정답 전체는 사용자가 명시적으로 요청한 경우에만 공개한다.
- 실패한 테스트를 숨기지 않는다.
- 문제 오류를 사용자의 실수로 처리하지 않는다.
- 사용자 풀이 파일은 리뷰 대상으로만 읽는다.
- `.reference/`는 비공개 로컬 자료로 취급한다. 저장소 문서에 개인정보를
  인용하거나 복사하지 않는다.

## 트랙 구조

언어와 직무를 기준으로 문제를 분리한다.

```text
{language}/{role}/day-NN/NN-slug/
```

사용 가능한 값:

| 구분 | 값 |
| --- | --- |
| `{language}` | `typescript`, `python` |
| `{role}` | `frontend`, `backend`, `fullstack` |

공개 문제:

```text
exercises/{language}/{role}/day-NN/NN-slug/
  README.md
  starter.ts 또는 starter.py
  public.test.ts 또는 test_public.py
```

사용자 풀이:

```text
solutions/user/{language}/{role}/day-NN/NN-slug/
  solution.ts 또는 solution.py
```

출제자 전용 자료는 학습자 화면에서 덜 보이도록 `.authoring/` 아래에 둔다.

```text
.authoring/solutions/reference/{language}/{role}/day-NN/NN-slug/
  solution.ts 또는 solution.py
.authoring/explanations/{language}/{role}/day-NN/NN-slug/
  README.md
.authoring/hidden-tests/{language}/{role}/day-NN/NN-slug/
  hidden.test.ts 또는 test_hidden.py
```

공개 테스트에서 정답 코드를 불러오지 않는다. 숨겨진 테스트는 리뷰할 때만
사용자 풀이를 불러올 수 있다.

## 언어별 방향

### TypeScript

- 프론트엔드와 풀스택 문제 비중을 충분히 둔다.
- 배열과 객체 가공, 타입 좁히기, 불변성, 비동기 처리, 테스트 작성을 다룬다.
- 백엔드 문제에서는 API 입력 검증, 오류 처리, 데이터 변환도 다룬다.

### Python

- 알고리즘과 자료구조 문제 비중을 충분히 둔다.
- 문자열, 리스트, 딕셔너리, 집합, 정렬, 탐색, 그래프, 동적 계획법을 다룬다.
- 백엔드와 풀스택 문제에서는 데이터 처리, SQL 연계 사고, API 로직도 다룬다.

언어별 방향은 기본값이다. 실제 문제 비율은 지원 직무, 목표 기업, 채용
플랫폼의 시험 유형, 오답 기록에 맞추어 조절한다.

## 문제 수 조절

처음부터 많은 문제를 한 번에 제시하지 않는다. 초기에는 풀이와 상세 리뷰에
집중하고, 사용자의 숙련도가 높아지면 문제 수를 점진적으로 늘린다.

| 단계 | 한 번에 제시할 문제 수 | 적용 기준 |
| --- | ---: | --- |
| 입문 | 1~2문제 | 학습 시작 시 기본값 |
| 기초 | 2~3문제 | 최근 풀이에서 기본 개념 설명이 안정적일 때 |
| 중급 | 3~5문제 | 반복 실수가 줄고 복잡도와 엣지 케이스를 설명할 수 있을 때 |
| 실전 | 6~8문제 | 제한 시간 안에 혼합 문제를 풀 준비가 되었을 때 |
| 모의 테스트 | 8~12문제 | 실제 시험에 가까운 연습이 필요할 때 |

문제 수를 늘릴 때는 최근 풀이의 정답률만 보지 않는다. 시간 복잡도와 공간
복잡도 설명, 엣지 케이스 발견 능력, 반복 실수 여부, 코드 리뷰 문제의 오류
원인 설명도 함께 확인한다.

설명이 부족하거나 같은 실수가 반복되면 문제 수를 유지하거나 줄이고, 같은
개념의 작은 변형 문제를 제시한다.

## 문제 출처 기록

채용 공고, 코딩테스트 플랫폼, 공개된 문제 유형을 참고했다면 각 문제의
`README.md`에 다음 메타정보를 남긴다.

```text
- 언어:
- 직무:
- 난이도:
- 예상 풀이 시간:
- 대비 유형:
- 참고 범위:
```

공개적으로 확인할 수 없는 기업 기출 문제라고 단정하지 않는다. 원문을
복제하지 않고 학습 목적에 맞게 변형한다.

## 풀스택 포트폴리오 참고

풀스택 트랙은 `docs/fullstack-tasktree-baseline.md`를 참고한다. TaskTree의
코드를 그대로 복제하지 않고 기술과 리뷰 관점을 일반화하여 문제를 만든다.
초기에는 작은 구현, 버그 수정, 코드 리뷰, 테스트 작성 문제를 섞는다.

## 사용자 풀이 리뷰 순서

Claude Code가 먼저 리뷰 초안을 만들고 Codex가 다시 검토한다. 사용자에게는
다음 순서로 상세히 설명한다.

1. 정답 여부
2. 누락된 요구사항
3. 실패하는 입력 예시
4. 엣지 케이스
5. 언어별 타입 또는 런타임 안전성
6. 원본 배열, 객체, 리스트, 딕셔너리 변경 여부
7. 시간 복잡도와 공간 복잡도
8. 가독성
9. 테스트 부족
10. 오류 원인
11. 개선 방향
12. 다음에 풀 변형 문제 또는 보완 학습 주제

사용자가 명시적으로 요청하지 않는 한 수정된 전체 코드를 제공하지 않는다.
